# Rust RAG精度自動向上メカニズム (Self-Improving Loop)

RAGの精度を「自動的に」向上させるための、Rustバックエンドにおけるアーキテクチャ設計指針です。

## 1. 精度向上の3つの階層

自動化された向上の仕組みは、以下の3段階で構成されます。

### A. 実行時補正 (Online Self-Correction)
ユーザーのクエリに対して、回答を生成する直前にその妥当性を検証し、必要に応じて「やり直す」仕組みです。
- **実装**: `rig` のエージェントを使用。
- **フロー**: 検索 -> コンテキスト評価(LLM) -> 情報不足ならクエリを変えて再検索 -> 生成。

### B. 継続的評価 (Offline Evaluation - RAGAS)
開発・運用フェーズで、システムの「弱点」を自動的に特定する。
- **仕組み**: 毎日またはデプロイごとに、過去のクエリ（または合成データ）に対して RAGAS 指標を計算。
- **自動化**: スコアが低下したクエリを「不適合リスト」としてキューイングします。

### C. フィードバックループとチューニング (Optimization Loop)
評価結果に基づき、検索パラメータや埋め込みを調整する仕組みです。

---

## 2. 実装アーキテクチャ (Rust Implementation)

### 成功・失敗ログの永続化
PostgreSQLに「LLMによる自己評価結果」を保存し、不適合データを蓄積します。

```sql
CREATE TABLE rag_feedback (
    id SERIAL PRIMARY KEY,
    query TEXT,
    context TEXT,
    answer TEXT,
    faithfulness_score FLOAT, -- RAGAS指標
    relevance_score FLOAT,
    is_failed BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 自動チューニングエージェント (The Optimizer)
失敗事例が一定数溜まった際に、バックグラウンドジョブが以下を実行します。

1. **原因分析**: 失敗したクエリをLLMに渡し、「なぜ検索に失敗したか（チャンクが短すぎる、キーワードが足りない等）」を分析。
2. **チャンクの再構築**: 失敗したドキュメントに対し、より広い文脈を持たせた「メタデータ付きチャンク」を自動生成してDBを更新。
3. **プロンプトの自動洗練**: 回答不備が多い場合、システムプロンプトに「よくある間違いの回避策」を自動で追記（DSPy風のアプローチ）。

---

## 3. 実装のロードマップ

1. **ステップ1 (評価の自動化)**: 推論ごとに `rig` エージェントでスコアリングし、結果を `rag_feedback` テーブルに書き込む。
2. **ステップ2 (不適合分析)**: ダッシュボード（WASM Leapos）上で、スコアの低いクエリを一覧表示し、LLMによる「改善アドバイス」を添える。
3. **ステップ3 (自動更新)**: 頻繁に外れるキーワードを `ts_vector` の辞書に動的に追加したり、チャンクサイズを自動調整する worker を実装する。
